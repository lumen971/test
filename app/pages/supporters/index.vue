<script setup lang="ts">import type { SupporterMonth } from '../../../shared/types/content'; useSeoMeta({ title: '星光名錄', description: '感謝每一位支持花火流明持續創作與提供靈性服務的贊助人。' }); const { data } = await useFetch<SupporterMonth>('/api/supporters/latest')</script>
<template><div v-if="data" class="page-wrap supporters-page"><SectionHeading eyebrow="STARDUST SUPPORTERS" title="星光名錄" :description="`${data.label}，謝謝每一道讓旅程得以延續的光。名單將由 Notion 自動同步更新。`" /><div class="support-tiers"><section v-for="group in data.groups" :key="group.tier" class="tier-card"><span>✦</span><h2>{{ group.tier }}</h2><ul><li v-for="member in group.members" :key="member">{{ member }}</li></ul></section></div><p class="privacy-note">名單僅顯示同意公開的名稱；你可以隨時在 Notion 調整顯示內容與排序。</p></div></template>
