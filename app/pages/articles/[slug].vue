<script setup lang="ts">const route = useRoute(); const { data: article } = await useFetch(`/api/articles/${route.params.slug}`); if (!article.value) throw createError({ statusCode: 404, statusMessage: '找不到這篇文章' }); useSeoMeta({ title: () => article.value?.title, description: () => article.value?.excerpt })</script>
<template><article v-if="article" class="article-page"><NuxtLink to="/articles">← 返回靈感札記</NuxtLink><p class="eyebrow">{{ article.category }} · {{ article.publishedAt }}</p><h1>{{ article.title }}</h1><p class="article-intro">{{ article.excerpt }}</p><div class="prose"><p>這是 Notion 文章內容的版面預覽。接入你的 Notion Data Source 後，這裡會呈現完整文章段落、標題、清單、引言與圖片。</p><blockquote>每一次覺察，都是把散落在外的注意力，溫柔地帶回自己。</blockquote><h2>讓狀態先被看見</h2><p>真正的整理並不是催促自己立即變好，而是先辨認當下正在發生什麼。當內容同步功能完成後，你只需要在 Notion 編輯與發布文章。</p></div></article></template>
